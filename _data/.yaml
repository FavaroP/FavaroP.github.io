# Well-formatted site data should be placed here. The Jekyll engine will autoload all data files in this directory and they will be accesible via `site.data`. If there's a file `members.yml`, then you can access contents of the file through `site.data.members`.
